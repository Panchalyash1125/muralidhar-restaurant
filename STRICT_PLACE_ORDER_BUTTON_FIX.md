# Strict Place Order Button Fix

Fixed mobile accidental order placement.

- Tapping outside the form to dismiss the keyboard can never place an order.
- Keyboard Enter/Go/Arrow cannot place an order.
- The order is placed only when the touch/pointer starts on the orange **Place Order** button and the button receives the matching click.
- Added duplicate-click protection.
